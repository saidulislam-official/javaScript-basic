function TenTimes(number){
    const result = number * 10;
    return result;
}

function cutHalf(number){
    const half = number / 2;
    return half;
}

// TenTimes(5);

const output = TenTimes(10); 

console.log(output);